import type { NextPage } from 'next';

import Register from 'modules/authenticate/components/Register';

const RegisterPage: NextPage = () => {
  return <Register />;
};

export default RegisterPage;
